


/* My first program in Swift 4 */
var myString = "Hello, World!"

print(myString)


// f you create the same program for iOS playground, then it will include import 
// UIKit and the program will look as follows

import UIKit
var myString = "Hello, World!"
print(myString)


//===========================Import in Swift 4===========================


// You can use the import statement to import any Objective-C framework (or C library) directly into
//  your Swift 4 program. For example, the above import cocoa statement makes all Cocoa libraries,
//  APIs, and runtimes that form the development layer for all of OS X, available in Swift 4


///======================= Tokens in Swift 4=========================/
// A Swift 4 program consists of various tokens and a token is either a keyword, an identifier, a constant, a string literal, 
// or a symbol. For example, the following Swift 4 statement consists of three tokens
print("test!")
The individual tokens are:
print("test!")


//==========================Keywords used in declarations=================

// Class   	deinit  	Enum    	extension
// Func    	import  	Init    	internal
// Let     	operator	private	    protocol
// public  	static	        struct          	subscript
// typealias   	var	


//======================Keywords used in statements===================


// break	        case	continue	default
// do           	else	fallthrough	for
// if	                in	return	switch
// where    	while\
//=================================Keywords used in expressions and types================

// as	    dynamicType	    false	is
// nil     	self	        Self	super
// true	_COLUMN_	        _FILE_	_FUNCTION_
// _LINE_

//=========================Keywords used in particular contexts=============/

// associativity    	convenience	    dynamic	    didSet
// final            	get	infix	inout
// lazy	left        	mutating	none
// nonmutating      	optional	override	postfix
// precedence	        prefix	Protocol	required
// right	            set	Type	unowned
// weak	willSet

